# Servidor

Comandos para iniciar o site do zero:

```sh
# Ter o node instalado
# Instalar todos os pacotes
npm i
# Configure o .env.local
npm run migrate
npm run seed # Seed é opcional

# Build do next
npm run build
npm run start # Apenas para teste
```